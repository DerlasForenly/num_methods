function Gauss_Seidel(matrix, b) {
    let x = [...b]
    matrix = createCopy(matrix)
    let n = matrix.length
    b = [...b]
    
    for (let i = 0; i < n; i++) {
        x[i] == 0
    }

    

    return x
}
